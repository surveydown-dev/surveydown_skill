# Connecting a database to a surveydown survey

> 🚧 **Under construction.** This guide is not written yet.

Will cover storing responses in an external PostgreSQL database (e.g. Supabase),
configuring credentials, and switching the survey from `mode: preview` to
`mode: database`.

In the meantime, see the official docs: <https://surveydown.org/docs/storing-data>
